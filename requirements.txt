# BBEATSx reproducible environment (see bbeatsx/README.md).
# Create the project venv and install with:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# stochtree provides the production C++ forest sampler. The exact pin is
# deliberate: BBEATSx's low-level observation-weight contract is tested against
# 0.4.4 and must be re-audited before an upgrade.

numpy>=1.23
scipy>=1.9
pandas>=1.5
scikit-learn
pybind11
pytest>=7.0
matplotlib>=3.5
stochtree==0.4.4
